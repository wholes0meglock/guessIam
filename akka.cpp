#include <bits/stdc++.h>
using namespace std;
#define int long long
void help() {
    int n,k;
    cin >> n >> k;
    vector<int> vec;
    int sum = 0;
    for(int i = 0; i < n; i++)
    {
        int num;
        cin >> num;
        vec.push_back(num);
        sum += num;
    }
    set<int> s;
    
}
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long double t;
    cin >> t;
    while(t--)
    { help();}
}
